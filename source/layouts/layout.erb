<!doctype html>
<html>
  <head>
    <!-- Use title if it's in the page YAML frontmatter -->
    <title><%= data.page.title || "Out of my mind..." %></title>
    <meta charset="utf-8" />
    <meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1' />
    <%= stylesheet_link_tag 'all' %>
    </style>
  </head>
  <body>
  <div class="page">

    <nav>
        <%= image_tag 'avatar.png', height: 210, width:210 %>
        <blockquote>
          I take bits from bucket a, twiddle them a little and put
          them in bucket b. Sometimes, I put them back in bucket a.
        </blockquote>
    </nav>
    <div class="content">
      <%= yield %>

    <footer>
      <div class="recent">
      <h2>Recent Articles</h2>
      <ol>
        <% blog.articles[0...10].each do |article| %>
          <li><%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %></span></li>
      <% end %>
      </ol>

    </div>
  <div class='by_year'>
    <h2>By Year</h2>
    <ol>
      <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
        <li><%= link_to year, blog_year_path(year) %> (<%= articles.size %>)</a></li>
  <% end %>
  </ol>
</div>
</footer>
    </div>
</div>
  </body>
</html>
